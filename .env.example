# Copie para .env (não compartilhe o .env) e rode: node --env-file=vyzor-local/.env vyzor-local/servir.mjs
BRAPI_TOKEN=
HGBRASIL_KEY=
# Google Maps Places (New V2) via RapidAPI — mesma chave do Reino no Babel OS (vault: reino_rapidapi_maps)
RAPIDAPI_MAPS_KEY=
